# Análisis exploratorio de PCA para detección de exoplanetas

## Requerimientos

Este programa fue ejecutado con `Python 3.12.12` y requiere la biblioteca *Vortex Image Processing* (VIP).

```bash
$ pip install vip_hci
```

Adicionalmente, se utilizaron las bibliotecas

```bash
numpy
matplotlib
```

## Ejecución

El programa se ejecuta una sola vez y reproduce exactamente lo especificado en la presentación. Mayores cambios requerirían modificaciones en el código. El código está comentado y estructurado en "celdas", por lo que se recomienda utilizar `Spyder` para editarlo.

```bash
python experiment.py
```

Esto generará imágenes en el directorio `images`, las cuales corresponden a los valores de SNR de la primera detección, cada una de las imágenes filtradas aplicando las
